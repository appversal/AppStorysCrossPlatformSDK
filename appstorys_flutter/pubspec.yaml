name: appstorys_flutter
description: "AppStorys Flutter SDK — display campaigns (Banner, Stories, PiP, Modal, Spin Wheel, Scratch Card, CSAT, Survey and more) in any Flutter app."
version: 1.0.0-alpha01
homepage: https://appstorys.com
repository: https://github.com/appversal/appstorys-flutter-sdk

environment:
  sdk: ^3.11.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  http: ^1.2.2
  lottie: ^3.1.3
  path_provider: ^2.1.4
  percent_indicator: ^4.2.4
  plugin_platform_interface: ^2.0.2
  share_plus: ^10.0.3
  shared_preferences: ^2.3.2
  video_player: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.appversal.appstorys_flutter
        pluginClass: AppstorysFlutterPlugin
      ios:
        pluginClass: AppstorysFlutterPlugin

  assets:
    - lib/assets/icons/cross.png
    - lib/assets/icons/maximize.png
    - lib/assets/icons/minimize.png
    - lib/assets/icons/mute.png
    - lib/assets/icons/unmute.png
    - lib/assets/icons/share.png

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
