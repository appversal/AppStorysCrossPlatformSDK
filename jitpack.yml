jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.4-tem
  - sdk use java 17.0.4-tem

install:
  - ./gradlew :shared-core:publishToMavenLocal -x test
  - ./gradlew :app:appstorys:publishToMavenLocal -x test
